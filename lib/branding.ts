export const schoolName =
  process.env.NEXT_PUBLIC_SCHOOL_NAME?.trim() || "WBTE";

export const schoolLogoUrl =
  process.env.NEXT_PUBLIC_SCHOOL_LOGO_URL?.trim() || "";

export const publicContactEmail =
  process.env.NEXT_PUBLIC_CONTACT_EMAIL?.trim() || "feedback.wbtes@gmail.com";
